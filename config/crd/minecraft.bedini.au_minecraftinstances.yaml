---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: minecraftinstances.minecraft.bedini.au
spec:
  group: minecraft.bedini.au
  names:
    kind: MinecraftInstance
    listKind: MinecraftInstanceList
    plural: minecraftinstances
    shortNames:
    - mci
    singular: minecraftinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.version
      name: Version
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.players.online
      name: Players
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MinecraftInstance is one Minecraft server.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MinecraftInstanceSpec is the desired state of a server.
            properties:
              autostart:
                default: true
                description: Autostart makes the supervisor relaunch the server on
                  boot.
                type: boolean
              configFiles:
                items:
                  description: ConfigFileSpec is a file on the PVC sourced from a
                    ConfigMap or Secret.
                  properties:
                    configMapKeyRef:
                      description: Selects a key from a ConfigMap.
                      properties:
                        key:
                          description: |-
                            The key to select from the ConfigMap's Data field.
                            Keys in the BinaryData field are not currently propagated to container env vars.
                          type: string
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                        optional:
                          description: Specify whether the ConfigMap or its key must
                            be defined
                          type: boolean
                      required:
                      - key
                      type: object
                      x-kubernetes-map-type: atomic
                    merge:
                      default: replace
                      description: MergeMode says how a config file is written.
                      enum:
                      - replace
                      - properties
                      type: string
                    path:
                      description: Path is relative to the instance directory.
                      type: string
                    secretKeyRef:
                      description: SecretKeySelector selects a key of a Secret.
                      properties:
                        key:
                          description: The key of the secret to select from.  Must
                            be a valid secret key.
                          type: string
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                        optional:
                          description: Specify whether the Secret or its key must
                            be defined
                          type: boolean
                      required:
                      - key
                      type: object
                      x-kubernetes-map-type: atomic
                  required:
                  - path
                  type: object
                  x-kubernetes-validations:
                  - message: set exactly one of configMapKeyRef or secretKeyRef
                    rule: has(self.configMapKeyRef) != has(self.secretKeyRef)
                type: array
              flavour:
                description: FlavourSpec selects the server software. Exactly one
                  field must be set.
                properties:
                  fabric:
                    description: Fabric runs the Fabric launcher jar.
                    properties:
                      installerVersion:
                        type: string
                      loaderVersion:
                        type: string
                    type: object
                  forge:
                    description: Forge runs a MinecraftForge build (1.17 and later).
                    properties:
                      build:
                        type: string
                    type: object
                  paper:
                    description: Paper runs a PaperMC build.
                    properties:
                      build:
                        format: int64
                        type: integer
                      channel:
                        default: stable
                        description: Channel is the least mature channel to accept
                          when Build is unset.
                        enum:
                        - stable
                        - beta
                        - alpha
                        type: string
                    type: object
                  vanilla:
                    description: Vanilla runs the Mojang server jar.
                    type: object
                type: object
                x-kubernetes-validations:
                - message: exactly one flavour must be set
                  rule: '[has(self.vanilla), has(self.fabric), has(self.paper), has(self.forge)].filter(x,
                    x).size() == 1'
              java:
                description: JavaSpec selects the JRE image.
                properties:
                  image:
                    description: |-
                      Image is a full image reference. When empty the operator derives one
                      from the resolved Java major with its image template.
                    type: string
                type: object
              jvm:
                description: JVMSpec configures the Java process.
                properties:
                  env:
                    additionalProperties:
                      type: string
                    description: Env is added to the process environment.
                    type: object
                  extraArgs:
                    description: ExtraArgs are appended verbatim before the launch
                      target.
                    items:
                      type: string
                    type: array
                  maxMemoryMiB:
                    default: 2048
                    format: int32
                    type: integer
                  minMemoryMiB:
                    default: 1024
                    format: int32
                    type: integer
                type: object
              metrics:
                description: MetricsSpec configures scraping.
                properties:
                  modEndpoint:
                    description: ModEndpoint is scraped when set.
                    properties:
                      path:
                        default: /metrics
                        type: string
                      port:
                        format: int32
                        type: integer
                    required:
                    - port
                    type: object
                  statsExporter:
                    description: StatsExporter runs the world-stats exporter as a
                      separate pod.
                    type: boolean
                type: object
              mods:
                items:
                  description: ModSpec is one jar in the mods directory.
                  properties:
                    digest:
                      description: DigestSpec is an expected file digest.
                      properties:
                        algorithm:
                          enum:
                          - sha1
                          - sha256
                          - sha512
                          type: string
                        value:
                          description: Value is lower-case hex.
                          type: string
                      required:
                      - algorithm
                      - value
                      type: object
                    modrinth:
                      description: ModrinthSource identifies a Modrinth version.
                      properties:
                        project:
                          description: Project is the Modrinth project id or slug.
                          type: string
                        version:
                          description: Version is the Modrinth version id or version
                            number.
                          type: string
                      required:
                      - project
                      - version
                      type: object
                    name:
                      description: Name identifies the mod in status and events.
                      type: string
                    url:
                      description: URL downloads the jar directly. Digest is required
                        with URL.
                      type: string
                  required:
                  - name
                  type: object
                  x-kubernetes-validations:
                  - message: set exactly one of modrinth or url
                    rule: has(self.modrinth) != has(self.url)
                type: array
              podOverrides:
                description: |-
                  PodOverrides is a strategic merge patch applied to the generated pod
                  template (sidecars, sysctls, volumes, securityContext).
                type: object
                x-kubernetes-preserve-unknown-fields: true
              resources:
                description: ResourceRequirements describes the compute resource requirements.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              restartPolicy:
                default: manual
                description: RestartPolicy says what happens on config drift.
                enum:
                - manual
                - automatic
                type: string
              service:
                description: ServiceSpec configures the Services.
                properties:
                  extraPorts:
                    items:
                      description: |-
                        ExtraPort is an additional port on the game Service (voice chat,
                        Bedrock).
                      properties:
                        name:
                          type: string
                        port:
                          format: int32
                          type: integer
                        protocol:
                          default: TCP
                          description: Protocol defines network protocols supported
                            for things like container ports.
                          type: string
                      required:
                      - name
                      - port
                      type: object
                    type: array
                  game:
                    description: GameServiceSpec configures the player-facing Service.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        type: object
                      port:
                        default: 25565
                        format: int32
                        type: integer
                      type:
                        default: LoadBalancer
                        description: Service Type string describes ingress methods
                          for a service
                        type: string
                    type: object
                type: object
              stopped:
                description: |-
                  Stopped keeps the server process stopped while the pod stays up, for
                  maintenance. The operator stops a running server and does not start
                  it until this is cleared.
                type: boolean
              storage:
                description: StorageSpec describes the instance PVC.
                properties:
                  existingClaim:
                    description: ExistingClaim mounts a claim the operator does not
                      own.
                    type: string
                  retainOnDelete:
                    default: true
                    description: |-
                      RetainOnDelete keeps the operator-created claim when the instance is
                      deleted.
                    type: boolean
                  size:
                    anyOf:
                    - type: integer
                    - type: string
                    description: Size of the claim the operator creates.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  storageClassName:
                    type: string
                  subPath:
                    description: SubPath inside the claim that holds the instance
                      directory.
                    type: string
                type: object
                x-kubernetes-validations:
                - message: size applies only to an operator-created claim
                  rule: '!(has(self.existingClaim) && has(self.size))'
              supervisor:
                description: SupervisorSpec pins the supervisor.
                properties:
                  image:
                    description: Image overrides the operator's default supervisor
                      image.
                    type: string
                type: object
              upgrade:
                description: UpgradeSpec guards version changes.
                properties:
                  allowDowngrade:
                    type: boolean
                  backupBeforeUpgrade:
                    default: true
                    type: boolean
                  force:
                    description: Force proceeds despite mod incompatibility.
                    type: boolean
                type: object
              version:
                description: Version is the Minecraft version, e.g. "26.3".
                minLength: 1
                type: string
            required:
            - flavour
            - version
            type: object
          status:
            description: MinecraftInstanceStatus is the observed state.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSave:
                format: date-time
                type: string
              managementProtocolVersion:
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: Phase is a one-word summary for kubectl.
                type: string
              players:
                description: PlayersStatus is the current player count.
                properties:
                  max:
                    format: int32
                    type: integer
                  online:
                    format: int32
                    type: integer
                required:
                - online
                type: object
              resolved:
                description: ResolvedStatus records what the spec resolved to.
                properties:
                  forgeBuild:
                    type: string
                  installerVersion:
                    type: string
                  javaImage:
                    type: string
                  javaMajor:
                    format: int32
                    type: integer
                  launchSpecHash:
                    description: LaunchSpecHash is the supervisor's hash of the launch
                      spec in effect.
                    type: string
                  loaderVersion:
                    type: string
                  mods:
                    items:
                      description: ResolvedMod is one installed mod.
                      properties:
                        digest:
                          type: string
                        file:
                          type: string
                        name:
                          type: string
                      required:
                      - file
                      - name
                      type: object
                    type: array
                  paperBuild:
                    format: int64
                    type: integer
                  serverJar:
                    description: ResolvedFile is a file the operator installed.
                    properties:
                      digest:
                        type: string
                      path:
                        type: string
                    required:
                    - path
                    type: object
                  version:
                    type: string
                type: object
              supervisorVersion:
                type: string
              upgrade:
                description: UpgradeStatus follows a world upgrade.
                properties:
                  phase:
                    type: string
                  progress:
                    description: Progress is 0 to 100.
                    format: int32
                    type: integer
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
